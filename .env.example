# =============================================================================
# Kolavi Studio - Environment variables (EXAMPLE ONLY)
# =============================================================================
# Copy this file to .env.local and fill in YOUR values.
# NEVER commit .env.local or any file containing real API keys, URLs, or tokens.
# .env.local is gitignored; this file is safe to commit (placeholders only).
# =============================================================================

# Public site URL (canonical, OG, sitemap, RSS).
# Local: http://localhost:3000  |  Production: your actual domain
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Organization schema address (for Rich Results / Knowledge Panel).
# When set, these appear in the site-wide Organization JSON-LD and clear "missing optional fields" in Rich Results Test.
# Set in .env.local for local dev. For production (e.g. Vercel), add the same vars in Project → Settings → Environment Variables, then redeploy.
# NEXT_PUBLIC_ORG_STREET_ADDRESS=123 Main St
# NEXT_PUBLIC_ORG_ADDRESS_LOCALITY=City Name
# NEXT_PUBLIC_ORG_POSTAL_CODE=12345

# Optional: build/deploy time (ISO date) for sitemap lastmod. Vercel sets VERCEL_BUILD_COMMIT_TIMESTAMP automatically.
# BUILD_TIMESTAMP=2026-02-06T00:00:00.000Z

# WordPress GraphQL endpoint for headless blog.
# When set, blog data is loaded from WordPress; when unset, sample data is used.
# Use your own WordPress URL – never commit a real production URL here.
NEXT_PUBLIC_WP_GRAPHQL_URL=https://your-wordpress-site.com/graphql

# Google Analytics 4 Measurement ID. When set, GA4 scripts load on every page.
# Optional; leave unset or commented to disable analytics.
# Use your own GA4 ID – never commit a real measurement ID here.
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional: Google Tag Manager (if you add support in layout.tsx).
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# -----------------------------------------------------------------------------
# Contact form (optional – use one; precedence: Typeform > Tally > Google Forms)
# -----------------------------------------------------------------------------
# Typeform: create a form at typeform.com, Share → Embed, copy the form ID from the
# embed code (data-tf-widget="XXXXX" → use XXXXX). Example: 01KGJEJHFEM5Q5S88DS411X006
# NEXT_PUBLIC_TYPEFORM_EMBED_URL=01KGJEJHFEM5Q5S88DS411X006

# Tally: create a form at tally.so, Share → Get the code → copy the embed URL
# (e.g. https://tally.so/embed/xxxxx). Set it here to show the Tally form on /contact.
# NEXT_PUBLIC_TALLY_FORM_EMBED_URL=https://tally.so/embed/your-form-id

# Google Forms: create a form, Send → embed HTML, copy the iframe src URL
# (e.g. https://docs.google.com/forms/d/e/1FAIpQLS.../viewform?embedded=true).
# NEXT_PUBLIC_GOOGLE_FORM_EMBED_URL=https://docs.google.com/forms/d/e/.../viewform?embedded=true

# On-demand revalidation: secret for POST /api/revalidate.
# When set, call this endpoint after creating/updating/deleting posts in WordPress
# so the frontend updates immediately (e.g. from a WP webhook or cron).
# REVALIDATE_SECRET=your-secret-here

# -----------------------------------------------------------------------------
# Dashboard & Database
# -----------------------------------------------------------------------------
# Supabase: URL and keys. Use anon key for auth (client + server); service_role for admin only.
# Important: When using the anon key (exposed client-side), enable Row Level Security (RLS)
# policies in Supabase to prevent unauthorized access to your tables. The anon key is public
# and must only be used with RLS enabled.
# Get these from Supabase dashboard → Project Settings → API.
# NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database connection string (Vercel Postgres or compatible provider).
# Get this from your hosting dashboard → Storage → Postgres → .env.local tab
# or from your database provider → Project Settings → Database → Connection string
DATABASE_URL=postgresql://user:password@host:port/database

# Admin dashboard secret. Use a long random string (e.g. generate with: openssl rand -hex 32)
# This protects your dashboard routes. Never commit the real value.
# ADMIN_SECRET=your-long-random-secret-here

# Required for unlock: code to clear login lockout (3 failed attempts = permanent lockout).
# Set a unique value. Enter it in the password field when locked to clear lockout.
# NOT stored in source code - must be set here.
# RATE_LIMIT_UNLOCK_CODE=your-unique-unlock-code

# -----------------------------------------------------------------------------
# Blog Generator Pipeline
# -----------------------------------------------------------------------------
# Primary content generation (drafts, gap-fill, humanize)
# ANTHROPIC_API_KEY=sk-...

# Search (discovery and competitor research)
# SERPER_API_KEY=...

# Secondary AI (data grounding, topic/style extraction)
# GEMINI_API_KEY=...
# Or: GOOGLE_AI_API_KEY=...

# Strategic analysis (brief generation, topic scoring)
# OPENAI_API_KEY=sk-...

# Content fetcher (fetch external content). Rate limits vary by plan.
# JINA_API_KEY=...
